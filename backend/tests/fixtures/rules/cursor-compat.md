---
description: Existing Cursor-style rule — only description + alwaysApply.
alwaysApply: true
---

# Cursor-style compatibility rule

This rule lacks explicit name/scope; the engine should derive ``name``
from the filename and treat ``alwaysApply: true`` as ``scope=[all]``.
