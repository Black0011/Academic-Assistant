"""AAF framework internals.

This package holds the LLM-agnostic building blocks. It must NOT import from
`backend.agents`, `backend.workflows`, or `backend.api` (enforced by the
aaf-python-style rule).
"""
