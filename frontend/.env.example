# Frontend env vars are baked into the bundle at build time.
# Copy to `.env` for local dev tweaks.

# Empty string => same-origin (recommended behind nginx).
# In dev, vite.config.ts proxies /api to http://127.0.0.1:8000.
VITE_API_BASE=
